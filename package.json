{
  "scripts": {
    "server": "npm start --prefix SERVER",
    "client": "npm start --prefix client",
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "vercel-build": "npm --prefix client install && npm --prefix client run build && rm -rf build && mv client/build build"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
